import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Add Expense",
	description: "Add a new expense to your personal expense tracker",
};

export default function AddExpensePage() {
	return <div>AddExpensePage</div>;
}
